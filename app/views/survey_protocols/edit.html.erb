<h1><%= @survey_protocol.title %></h1>

<h3>Sample Configurations</h3>

<p>Use the editor below to configure <b>sample-specific fields.</b></p>
<p><%= link_to "Click here to configure entry-specific fields.", edit_survey_protocol_entry_fields_path(@survey_protocol) %></p>

<div id='formbuilder'></div>

<script>
  Formbuilder.options.HTTP_ENDPOINT = "<%= survey_protocol_path(@survey_protocol) %>";
  Formbuilder.options.HTTP_METHOD = "PUT";
  new Formbuilder({
    selector: '#formbuilder',
    bootstrapData: <%= raw(@survey_protocol.sample_fields.to_json) || [] %>,
  });
</script>
